plugins {
    id 'java-library'
}

def versionMajor = 7
def versionMinor = 0
def versionPatch = 0

group 'com.christophecvb.touchportal.annotations'
version "${versionMajor}.${versionMinor}.${versionPatch}"

dependencies {
    testImplementation group: 'junit', name: 'junit', version: '4.12'
}
